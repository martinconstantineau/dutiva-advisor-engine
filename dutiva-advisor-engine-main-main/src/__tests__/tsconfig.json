{
  "extends": "../../tsconfig.json",
  "compilerOptions": {
    "rootDir": "../..",
    "types": ["node", "express", "vitest/globals"],
    "noEmit": true
  },
  "include": [
    "**/*.ts",
    "../**/*.ts"
  ],
  "exclude": [
    "../../node_modules",
    "../../dist",
    "../../coverage"
  ]
}
